# arXiv Manager - Environment Variables Example
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to git (it should be in .gitignore)

# ===================================================================
# User-Agent Configuration for arXiv API Requests
# ===================================================================
# arXiv requires a User-Agent with contact email to comply with their
# usage guidelines: https://info.arxiv.org/help/api/user-manual.html

# Option 1: Full User-Agent string (Highest priority)
# Uncomment and customize if you want complete control:
# ARXIV_UA=arxiv-manager/0.1 (mailto:your-email@example.com)

# Option 2: Email only (Priority 2 - recommended for most users)
# The system will automatically construct: "arxiv-manager/0.1 (mailto:EMAIL)"
# Uncomment and replace with your actual email:
# ARXIV_CONTACT_EMAIL=your-email@example.com

# Option 3: Default (if both above are unset)
# If you don't set either variable, the system uses:
# arxiv-manager/0.1 (mailto:2134893202@qq.com)

# ===================================================================
# Rebuild Script Configuration
# ===================================================================

# Number of days to fetch (default: 7)
# REBUILD_DAYS=7

# Use arXiv server clock (default: 1)
# Set to 0 to use local system time instead
# USE_SERVER_CLOCK=1

# Force specific data source (default: auto)
# Options: auto (tries API first, then HTML fallback) | api | html
# FORCE_SOURCE=auto

# ===================================================================
# Notes
# ===================================================================
# - The default email (2134893202@qq.com) is hardcoded in the app
# - For production or shared environments, set ARXIV_CONTACT_EMAIL
# - Temporary override: ARXIV_UA="custom UA" npm run rebuild
# - Check current UA: curl http://localhost:3000/api/admin/show-ua
